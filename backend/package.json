{
  "name": "rh-exercise-be",
  "version": "0.0.1",
  "private": true,
  "description": "tech assess ratehub",
  "main": "rha.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node rha.js",
    "startdb": "mongod --dbpath ./data/db &",
    "install": "chmod +x install; ./install"
  },
  "author": "TIO, abraham",
  "license": "private",
  "dependencies": {
    "express": "^4.17.3"
  }
}
